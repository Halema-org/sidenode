{
  "name": "@halema/sidenode-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Sidenode monorepo: core library, API service, SDK, and CLI",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
